# CLI

## Export

The `export` command writes all tasks to stdout. Choose the output
format with `--format` (default `json`).

## Purge

The `purge` command deletes archived tasks older than `--days`
(default 30).
